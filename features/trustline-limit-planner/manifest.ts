import { Wrench } from "lucide-react";
import type { FeatureManifest } from "@/core/registry/types";
export const manifest: FeatureManifest = {slug:"trustline-limit-planner",title:"Trustline Limit Change Planner",category:"assets",description:"Compare a pasted credit trustline snapshot with a proposed limit using exact seven-decimal amounts. Snapshot constraints cannot prove complete transaction validity, current authorization or live network state.",character:"Inspect locally, keep exact values.",status:"beta",icon:Wrench,networks:[],offline:true,keywords:["trustline", "limit", "planner"]};
